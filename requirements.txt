# Python packages installed into the lab image (pip), after AICON itself.
# AICON's own dependencies come from its pyproject.toml (see binder/Dockerfile).

# Keep AICON's numpy pin when other packages are resolved
numpy==1.26.4

# Simulation for the drawer demo (AICON README: robosuite 1.4.1)
robosuite==1.4.1
mujoco==3.1.6
# robosuite.wrappers imports h5py at package level but robosuite does not list it
h5py==3.12.1

# Imported by the AICON drawer tutorial but not listed in its pyproject.toml
loguru==0.7.2
lovely-tensors

# Notebook UI
ipywidgets
